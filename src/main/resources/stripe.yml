Gateways:
  Stripe-Services:
    info:
      api_url: "https://api.gatewayx.com"
    secrets:
      api_key: "your-api-key"
    fallback:
      timeout: 30
      retries: 3

  Mercado-Pago:
    info:
      api_url: "https://api.gatewayx.com"
    secrets:
      api_key: "your-api-key"
    fallback:
      timeout: 30
      retries: 3
